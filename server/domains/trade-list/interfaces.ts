export interface TradeItem {
  isNewRecord: boolean;
  apartName: string;
  address: string;
  buildedYear: number;
  householdsNumber: number;
  tradeDate: string;
  size: number;
  floor: number;
  tradeAmount: number;
  maxTradeAmount: number;
}
