export interface TradeItem {
  isNewRecord: boolean;
  apartName: string;
  buildedYear: string;
  householdsNumber: string;
  tradeDate: string;
  flatSize: string;
  areaSize: string;
  floor: string;
  tradeAmount: string;
  maxTradeAmount: string;
}
